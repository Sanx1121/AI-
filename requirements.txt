# --- GUI & async event loop ---
PySide6>=6.6.0
qasync>=0.27.0

# --- Audio capture (WASAPI loopback on Windows) ---
numpy>=1.26.0
pyaudiowpatch>=0.2.12.6

# --- Local ASR ---
faster-whisper>=1.0.0

# --- Cloud translation (DashScope Qwen-MT) ---
httpx>=0.27.0

# --- Config (Python < 3.11) ---
tomli>=2.0.0; python_version < "3.11"

# --- Optional: Silero VAD (falls back to Energy VAD if missing) ---
# torch>=2.0.0

# --- Dev / test ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
